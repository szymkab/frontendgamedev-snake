// NOTE: legacy package :(
//       fake export to not use the global variable
//       add some exports if you need
const createjs = window.createjs;

export const Stage = createjs.Stage;
export const Container = createjs.Container;
export const Shape = createjs.Shape;
export const Graphics = createjs.Graphics;
export const Ticker = createjs.Ticker;
export const Text = createjs.Text;

export default createjs;
